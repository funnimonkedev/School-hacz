/* press the "u" key to start and then awnser a question CORRECTLY then press the ";" key */
javascript:var speed11 = parseInt(prompt("miilisecond awnser speed preferably '200' =)"));var socket = null; var lastDataSent = null; var using = null; var answer = false; const nativeSend = window.WebSocket.prototype.send; window.WebSocket.prototype.send = function(data) { nativeSend.call(this, data); if (data != 2) { lastDataSent = data; } socket = this; }; document.onkeydown = (e) => { if (e.repeat) { return; } if (e.key == ";") { using = lastDataSent; alert("Saved."); } else if (e.key == "u") { if (answer) { answer = false; } else { answer = true; }; }; }; async function answerQuestion() { if (using && answer) { socket.send(using); }; await new Promise(resolve=>{ setTimeout(resolve, speed11); }); answerQuestion(); }; answerQuestion();javascript:void 0;
